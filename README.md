📚 Library Management System (Spring Boot)
Приложение для управления библиотечным фондом.
Система позволяет библиотекарям управлять книгами и читателями, отслеживать выдачу экземпляров, а также контролировать возвраты.

✅ Основные функции
Управление книгами:	добавление, редактирование, удаление, просмотр
Управление читателями: создание и учёт читателей
Выдача книг: привязка книги к читателю
Возврат книг: освобождение книги от владельца
аутентификация: доступ в систему только у библиотекарей

🛠 Используемый стек технологий
Backend: Java 17, Spring Boot, Spring MVC, Spring Data JPA, Spring Security
Database: PostgreSQL
View layer: Thymeleaf, HTML
Build system: Maven
Архитектура: MVC

🔐 Авторизация
В систему входит только библиотекарь.
Аутентификация реализована через Spring Security.

🧩 Структура проекта
src
└── main
├── java
│   └── ru.zyryanova
│        ├── Controller  → Web-логика
│        ├── Service     → Бизнес-логика
│        ├── Repository  → Доступ к данным
│        ├── Model       → JPA-сущности
│        └── Security    → Аутентификация
└── resources
├── templates       → Страницы Thymeleaf
└── application.properties 

🚀 Как запустить проект
Настройка базы данных PostgreSQL
Создайте базу:
CREATE DATABASE library_db;

Обновите настройки в application.properties:
spring.datasource.url=jdbc:postgresql://localhost:5432/library_db
spring.datasource.username=postgres
spring.datasource.password=your_password

Запуск:
mvn spring-boot:run

Приложение станет доступно по адресу: http://localhost:8080

🗺️ Маршруты (Endpoints)

🔐 AuthController
Метод	Путь	       Описание
GET	   /login	      Страница входа в систему
GET	   /registration  Страница регистрации нового библиотекаря
POST   /registration  Обработка формы регистрации
POST   /logout	      Выход из системы (обрабатывается Spring Security)
📚 BooksController

Метод	Путь	                Описание
GET	    /books	              Список всех книг (с пагинацией и сортировкой)
GET	    /books/new	          Форма для добавления новой книги
POST	/books	              Создание новой книги
GET	    books/{id}	          Просмотр информации о конкретной книге
GET	    /books/{id}/edit      Форма для редактирования книги
PATCH	/books/{id}	          Обновление данных книги
DELETE	/books/{id}	          Удаление книги
GET	    /books/search	      Страница поиска книг
POST	/books/search	      Выполнение поиска книг по названию
PATCH	/books/{id}/addPerson Выдать книгу читателю
👥 PeopleController

Метод	Путь                      Описание
GET	   /people	                Список всех читателей
GET	   /people/new	            Форма для добавления нового читателя
POST   /people	                Создание нового читателя
GET	   /people/{id}	            Просмотр информации о читателе и его книгах
GET	   /people/{id}/edit        Форма для редактирования данных читателя
PATCH  /people/{id}	            Обновление данных читателя
DELETE /people/{id}	            Удаление читателя
PATCH  /people/{book_id}/free	Освободить книгу (вернуть в библиотеку)
